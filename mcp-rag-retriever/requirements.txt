# MCP server framework.
# Pinned exactly: agentic_rag_answer reads the LibreChat conversation header via
# ctx.request_context.request.headers — an internal (non-public-API) surface of
# FastMCP. Pinning prevents a silent upgrade from breaking session grouping.
mcp==1.27.2

# Embeddings (same model as agentic-rag / vector-rag)
langchain-huggingface>=0.1.0,<1.0.0
sentence-transformers>=3.0.0,<4.0.0

# ClickHouse-native vector store client
clickhouse-connect>=0.8.0,<1.0.0
