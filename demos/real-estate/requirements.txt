# Real Estate Property Concierge demo
# Floor is >=4.10, which is the STRICTER of two independent requirements:
#   >=4.7  — Langfuse Cloud grants real-time ingestion off `x-langfuse-sdk-version`,
#            and only Python >= 4.7.0 qualifies.
#   >=4.10 — multi-modal dataset items / experiments (LangfuseMediaReference
#            hydration), which this demo's eval path uses.
# Needs Python >=3.10 — the SDK will not install on 3.9, still the macOS system python.
langfuse>=4.10,<5.0
anthropic>=0.40
python-dotenv>=1.0
# OpenAI — agent runs on GPT for the Claude-vs-GPT comparison
openai>=1.40

# Portal (the show-able app)
fastapi>=0.110
uvicorn[standard]>=0.29

# --- photo-audit path (multi-modal eval) -------------------------------------
# The listing-photo audit runs as a LangGraph graph so the demo has a real
# trajectory to trace (nodes, a tool call, a conditional self-correct branch).
langgraph>=1.0,<2.0
langchain-anthropic>=1.0,<2.0
# `langchain` is the META-package and is NOT optional: `langfuse.langchain`
# imports `langchain` directly, so installing only langchain-core (which
# langgraph pulls in) fails at import with ModuleNotFoundError. Found the hard
# way — do not "tidy" this away as a transitive dependency.
langchain>=1.0,<2.0
# NOTE: photo fixtures are rendered with the STDLIB (zlib + struct) on purpose —
# no Pillow. See data/photo_scenes.py for why.
