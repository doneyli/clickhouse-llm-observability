# Core LLM frameworks
langchain>=0.3.0,<1.0.0
langchain-anthropic>=0.3.0,<1.0.0
langchain-community>=0.3.0,<1.0.0

# Langfuse - LLM observability
langfuse>=4.7,<5.0

# MCP client & utilities
httpx>=0.27.0,<1.0.0
python-dotenv>=1.0.0,<2.0.0

# ClickHouse client — the refine loop's evidence runner (EXPLAIN + bounded
# read-only execution against sql.clickhouse.com); see sql_evidence.py
clickhouse-connect>=0.7,<1.0
